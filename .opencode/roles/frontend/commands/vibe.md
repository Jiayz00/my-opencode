---
description: 加载本角色的 vibe-core 工作流 skill 并按 6 步流程引导本次会话。
---

Load the `vibe-core` skill to start the workflow:

```
skill({ name: "vibe-core" })
```

然后按 6 步流程引导：需求澄清 → 规格与计划 → 权限门禁 → 开发与评审 → 验收测试 → 兼容性测试。
